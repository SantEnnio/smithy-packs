This is a Unity C# project. Editable code lives under `Assets/` (`*.cs`, `*.unity`, `*.prefab`, `*.asset`, `*.asmdef`). `Packages/`, `Library/`, `Temp/`, `Logs/`, `obj/` are read-only or generated. `*.meta` files are auto-generated GUID sidecars — never edit them by hand. To verify a build, call `unity_compile`. Bootstrap tool calls have ALREADY run (project layout + Unity scene/script/prefab graph summary). The OBSERVED list below already contains the filesystem layout AND the graph digest. Do NOT re-discover with `ls`, and do NOT re-call `unity_graph` with `scope=summary` — drill in with a different scope (`scene`, `script`, `prefab`, `orphans`) only when you need more detail.
